import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  Form: {
    padding: "2rem 3rem",
    display: "flex",
    flexDirection: "column",
  },
  Paper: {
    width: "100%",
    maxWidth: "650px",
  },
  FormControl: {
    width: "100%",
  },
});
